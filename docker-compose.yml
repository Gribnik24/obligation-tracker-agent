services:
  agent:
    build: .
    container_name: obligation-tracker-agent
    env_file: .env
    stdin_open: true
    tty: true
    volumes:
      - ./data:/app/data
    
  tests:
    build: .
    env_file: .env
    volumes:
      - ./:/app
    command: pytest tests/ -v